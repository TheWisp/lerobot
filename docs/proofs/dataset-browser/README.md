# Dataset browser: search, favorites, sort

Captured from a GUI booted on a random free port by `scripts/gui/screenshot_gui.py`,
on the branch's own code, against this workstation's real dataset cache (153
datasets). Cropped to the Datasets panel.

| File              | State                                                                 |
| ----------------- | --------------------------------------------------------------------- |
| `1-controls.png`  | Unfiltered: search box, sort select, favorites toggle, summary line   |
| `2-search.png`    | Query `aloha` — "14 of 153 datasets", and `14/153` on the source row  |
| `3-favorites.png` | Favorites-only with three starred — "3 of 153 datasets · 3 favorites" |
