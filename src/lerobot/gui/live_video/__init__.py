"""The Run tab's live camera video: the stages that carry a frame from the
tap to an encoded access unit, each forwarding the newest and holding
nothing. Design: ``gui/docs/live_camera_video.md``.
"""
